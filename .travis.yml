os: linux

language: python

python:
  - "2.6"
  - "2.7"

branches:
  only:
    - master

install:
  - pip install pyasmjit
  - pip install z3-solver
  - pip install nose
  - pip install .

script: nosetests
